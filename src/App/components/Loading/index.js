import { React } from "react";
import { StyledLoading } from "./styled";

const Loading = () => (
    <StyledLoading>
        Wait a second, we are loading exchange rates...
    </StyledLoading>
);

export default Loading;